# Docker 部署记录

**部署时间**: 2026-03-02 21:34  
**部署版本**: fund-system 基金详情页实时数据获取功能

---

## 服务状态

| 服务 | 容器名 | 端口 | 状态 |
|------|--------|------|------|
| MySQL | fund-mysql | 13306:3306 | ✅ Healthy |
| Redis | fund-redis | 16379:6379 | ✅ Healthy |
| Java 后端 | fund-api | 18080:8080 | ✅ Healthy |
| Python 采集 | - | 5001 | ✅ 运行中(宿主机) |

---

## 部署变更

### 1. Docker Compose 配置

- Java 服务添加 `COLLECTOR_URL` 环境变量
- 使用 `host.docker.internal:5001` 访问宿主机 Python 服务

### 2. Java 配置

- `collector.url` 支持环境变量覆盖
- 默认: `http://localhost:5001`
- Docker: `http://host.docker.internal:5001`

### 3. Python 采集服务

- 运行在宿主机端口 5001
- Java 容器通过 Docker 内部网络访问

---

## 验证命令

```bash
# 检查服务健康
curl http://localhost:18080/actuator/health

# 检查 Python 服务
curl http://localhost:5001/health

# 容器内访问测试
docker exec fund-api curl -s http://host.docker.internal:5001/health
```

---

## 注意事项

1. **Python 服务端口**: 5001（原 5000 被 macOS AirPlay 占用）
2. **容器网络**: Java 容器使用 `host.docker.internal` 访问宿主机
3. **数据持久化**: MySQL 和 Redis 数据使用 Docker volumes

---

**部署人员**: Assistant  
**Git 提交**: `7d31827`

---

## 更新记录

### 21:39 - 补充部署前端服务

**问题**: nginx 前端服务未启动

**解决**:
```bash
cd deploy && docker-compose up -d nginx
```

**验证**:
- ✅ nginx 运行中: http://localhost:8888
- ✅ 前端页面正常加载

### 完整服务列表

| 服务 | 容器名 | 端口 | 状态 |
|------|--------|------|------|
| Nginx (前端) | fund-nginx | 8888:80 | ✅ 运行中 |
| Java 后端 | fund-api | 18080:8080 | ✅ Healthy |
| MySQL | fund-mysql | 13306:3306 | ✅ Healthy |
| Redis | fund-redis | 16379:6379 | ✅ Healthy |
| Python 采集 | - | 5001 | ✅ 运行中(宿主机) |

---

## 访问地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:8888 |
| Java API | http://localhost:18080 |
| Python 采集 | http://localhost:5001 |
